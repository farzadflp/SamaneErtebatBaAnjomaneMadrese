<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samaneertebatbaanjomanemadrese">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ManagerAddCommunityActivity"></activity>
        <activity android:name=".ManagerProfileActivity" />
        <activity android:name=".ManagerVerifyParentActivity" />
        <activity android:name=".VerificationActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".NewConversationActivity" />
        <activity android:name=".InboxActivity" />
        <activity android:name=".ParentProfileActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>